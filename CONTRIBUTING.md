# Contributing

Issues and pull requests are welcome.

Before submitting a change:

1. Run `scripts/build-native.sh`.
2. Run `node --check usage-widget.js` if Node.js is available.
3. Open the built app and verify compact, hover, drag, light, and dark states.
4. Keep account data local and never add telemetry or credential collection.
